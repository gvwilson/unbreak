# Session Log: 2026-03-28T22:29:30Z

## Prompts Given

1. Start a new log file.
2. Create lesson files for the `perfcon` chapter using the same approach as
   `basicpy`, `interpy`, and `diot`.

## Actions Taken

1. Created this log file.
2. Read `perfcon/index.md`; found 5 exercises. The file has no `{: #id}` anchors
   yet, so slugs were derived from the headings:
   - "Repeated Scans"                  → `repscans`  → `repscans.py` + `repscans.txt`
   - "Subprocess Waiting for Input"    → `subproc`   → `subproc.py`
   - "Race Condition in Shared Counter"→ `race`      → `race.py`
   - "Multiprocessing Memory Model"    → `multiproc` → `multiproc.py`
   - "Mock Patched at Wrong Location"  → `mockpatch` → `mockpatch.py`
                                                        + `mockpatch_source.py`
                                                        + `mockpatch_user.py`
3. Created all files listed above in `perfcon/`.

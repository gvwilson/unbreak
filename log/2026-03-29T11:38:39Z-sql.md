# Session log: 2026-03-29T11:38:39Z — SQL chapter

## Task

Read SQL lessons from ~/learn/sql/*.py (six marimo notebooks covering basic SELECT,
filtering, aggregation/grouping, NULL handling, joins, and primary/foreign keys),
then create 20 SQL debugging exercises in a new `sql/` chapter.

## Source lessons read

| File | Topic |
|------|-------|
| 01_basic_select.py | SELECT, columns, ORDER BY, LIMIT/OFFSET, DISTINCT, calculations, aliases |
| 02_filter.py | WHERE, comparison operators, AND/OR/NOT |
| 03_aggregate_group.py | COUNT/SUM/AVG/MIN/MAX, GROUP BY, HAVING |
| 04_null.py | NULL values, IS NULL, IS NOT NULL, ternary logic, aggregation with nulls |
| 05_join.py | INNER JOIN, LEFT JOIN, COALESCE |
| 06_keys.py | Primary/foreign keys, self-joins, many-to-many, join tables |

## Databases required (not copied)

| Database | Tables used | Exercises |
|----------|-------------|-----------|
| penguins.db | penguins | 01–15 (sortcol through nullneq) |
| lab.db | job, work | 16–18 (innerleft, noonclause, coalescemiss) |
| survey.db | person, survey | 19–20 (joinswapped, selfjoin) |

### penguins table columns
species, island, sex, body_mass_g, flipper_length_mm, bill_length_mm, bill_depth_mm

### lab.db tables
- job: name TEXT, credits REAL
- work: person TEXT, job TEXT

### survey.db tables
- person: person_id TEXT PK, personal TEXT, family TEXT, supervisor_id TEXT FK→person
- survey: survey_id TEXT PK, person_id TEXT FK→person, start_date TEXT, end_date TEXT
- machine: machine_id TEXT PK, machine_type TEXT
- rating: person_id TEXT FK→person, machine_id TEXT FK→machine, level INTEGER

## Exercises created

| Slug | Topic | Bug |
|------|-------|-----|
| sortcol | ORDER BY | Sorts by species-then-island instead of island-then-species |
| offsetpage | LIMIT/OFFSET | OFFSET 11 skips 11 rows; should be OFFSET 10 for rows 11–15 |
| intdivision | Calculations | Integer division truncates body_mass_g / 1000 |
| distinctcols | DISTINCT | Extra column produces (species, sex) pairs instead of species alone |
| nullequal | NULL | WHERE sex = null never matches; use IS NULL |
| impossibleand | AND/OR | AND between two island conditions can never both be true |
| casevalue | String comparison | 'female' doesn't match stored 'FEMALE' |
| wherealiased | WHERE on alias | WHERE references SELECT alias before it is defined |
| andorprecedence | Operator precedence | Missing parentheses; AND binds tighter than OR |
| nonaggcol | GROUP BY | sex in SELECT but not in GROUP BY gives arbitrary values |
| wherenothavin | HAVING | Aggregate in WHERE clause raises error; use HAVING |
| groupbymissing | GROUP BY | island in SELECT missing from GROUP BY |
| countnull | count(*) vs count(col) | count(sex) skips null rows; use count(*) for total |
| avgmanual | Aggregation + null | sum/count(*) wrong denominator when nulls present |
| nullneq | IS NOT NULL | WHERE sex != null returns no rows; use IS NOT NULL |
| innerleft | JOIN type | INNER JOIN drops people who only did uncredited jobs |
| noonclause | JOIN | Missing ON clause produces Cartesian product |
| coalescemiss | COALESCE | LEFT JOIN sum is null for zero-credit people; need COALESCE |
| joinswapped | JOIN ON | ON condition joins survey.person_id to itself |
| selfjoin | Self-join | ON pa.person_id = pb.supervisor_id shows relationship backwards |

# Session Log: 2026-03-28T22:41:37Z

## Prompts Given

1. Create lesson files for `jsbrowser` using the same pattern as previous chapters,
   and update `jsbrowser/index.md` with `{: #id}` anchors and `[% inc ... %]`
   directives following the `diot/index.md` format.

## Actions Taken

1. Created this log file.
2. Read `jsbrowser/index.md`; derived slugs from headings:
   - "var Hoisting in Loops"           → `hoisting`     → `hoisting.html`
   - "Dead DOM Event Listener"         → `deaddom`      → `deaddom.html`
   - "this Binding in Callbacks"       → `thisbind`     → `thisbind.html`
   - "Promise Not Awaited"             → `unawaited`    → `unawaited.html`
   - "localStorage in Private Mode"    → `localstorage` → `localstorage.html`
   - "Missing Viewport Meta Tag"       → `viewport`     → `viewport.html`
   - "CORS Header Missing"             → `cors`         → `cors.html` + `cors_server.py`
   - "Browser Caching Stale JavaScript"→ `caching`      → `caching.html` + `caching.js`
   - "Missing Source Map"              → `sourcemap`    → `sourcemap.html`
                                                           + `sourcemap.min.js`
                                                           + `sourcemap_original.js`
   - "Content Security Policy Block"   → `csp`          → `csp.html`
3. Created all source files in `jsbrowser/`.
4. Updated `jsbrowser/index.md` with IDs and `[% inc ... %]` directives.

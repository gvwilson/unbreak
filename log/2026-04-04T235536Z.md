# 2026-04-04T235536Z

## Prompts

1. Read CLAUDE.md and all lesson index files; suggest four new sections suitable for the audience.
2. Add a `web` section with ten HTTP/API questions and a `polars` section with ten Polars questions.
3. Clarification: will each web example need its own server, or one shared server?
4. User provided an httpx/ASGI pattern for simulating HTTP interactions; asked to use it for web examples.
5. Approval to proceed with implementation.

## Actions

- Added `httpx>=0.27.0` and `polars>=1.0.0` to `pyproject.toml` dependencies.
- Ran `uv add httpx polars` to install packages (httpx 0.28.1, polars 1.39.3).
- Updated `README.md` to add `[HTTP and APIs](@/web/)` after jsbrowser and `[Polars](@/polars/)` after datasci in the chapter list.
- Created `web/index.md` with ten examples (nostatuscheck, jsonparam, urlparam, nopaginate, notimeout, created, ratelimit, putpatch, tokenurl, postredirect).
- Created ten Python source files in `web/` using `httpx.ASGITransport` + inline ASGI apps (corrected from deprecated `app=` keyword argument).
- Created `polars/index.md` with ten examples (selectdrop, nocollect, nullequal, castsilent, overwindow, innerjoin, aliasexpr, strptime, explodestring, withcolumnsdep).
- Created ten Python source files in `polars/`.
- Fixed `polars/castsilent.py` and `polars/strptime.py`: Polars 1.39 defaults to strict casting, so both examples were updated to use `strict=False` explicitly (making that the bug) and their explanations in `polars/index.md` were updated accordingly.
- Verified all examples produce expected output.

# Session log: 2026-03-29T18:03:05Z

## Task

Add a `shell` chapter with 20 debugging exercises for learners who have completed
~/shell-novice/episodes (01-intro through 07-find). Topics assumed known:
navigation, file operations, pipes/redirection, wildcards, for loops, scripts
($1/$2/$@), grep, find, cat/sort/wc/head/tail/cut/uniq.
Topics NOT assumed: if/else, while, functions, arithmetic, sed/awk, permissions,
environment variables.

## Slugs

| Heading | Slug | Bug |
|---------|------|-----|
| Numeric Sort | numericsort | sort without -n |
| Redirect Overwrites | overwrite | > in loop instead of >> |
| Wildcard Too Broad | wideglob | *.txt matches files that should stay |
| Unsorted Input to uniq | uniqsorted | uniq before sort misses duplicates |
| Unquoted Loop Variable | quotedvar | $f not "$f" breaks on spaces |
| Wrong Positional Parameter | wrongarg | $2 vs $1 |
| head Instead of tail | headtail | head gets first N, not last N |
| Too Many .. | dotdot | ../../ instead of ../ |
| Wrong wc Flag | wcflag | -w (words) instead of -l (lines) |
| Unquoted Glob in find | findglob | find -name *.csv shell-expands glob |
| Wrong cut Delimiter | cutdelim | -d, on tab-delimited file |
| Inverted grep | grepinvert | -v shows non-matches |
| Missing Script Argument | missingarg | $1 empty, wc reads stdin |
| Wrong cat Order | catorder | files concatenated in wrong order |
| Loop Clobbers on Re-run | loopclobber | >> accumulates across runs |
| sort Missing -n | sortn | sort -r alphabetical, not numeric reverse |
| head Off by One | headcount | head -n 10 gets 9 data rows + header |
| Unquoted $@ | quotedall | $@ splits on spaces in filenames |
| find Missing -type f | findtype | -name "*.log" matches directories too |
| cp Missing -r | cprecurse | cp dir/ fails without -r |

## Files created

shell/index.md
shell/numericsort.sh + shell/numericsort.txt
shell/overwrite.sh + shell/overwrite_a.dat shell/overwrite_b.dat shell/overwrite_c.dat
shell/wideglob.sh + shell/notes_jan.txt shell/notes_feb.txt shell/notes_archive.txt
shell/uniqsorted.sh + shell/species.csv
shell/quotedvar.sh
shell/wrongarg.sh
shell/headtail.sh + shell/headtail.txt
shell/dotdot.sh + shell/measurements.txt
shell/wcflag.sh + shell/wcflag.txt
shell/findglob.sh
shell/cutdelim.sh + shell/cutdelim.txt
shell/grepinvert.sh + shell/grepinvert.txt
shell/missingarg.sh
shell/catorder.sh + shell/section1.txt shell/section2.txt shell/section3.txt
shell/loopclobber.sh
shell/sortn.sh
shell/headcount.sh + shell/headcount.csv
shell/quotedall.sh
shell/findtype.sh
shell/cprecurse.sh

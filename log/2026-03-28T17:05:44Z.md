# Session Log: 2026-03-28T17:05:44Z

## Prompts Given

1. Start a new log file.
2. Read `basicpy/index.md`.
3. For each level-2 heading with an ID of the form `basicpy-something`, create
   `basicpy/something.py` containing code with the bug. Create input files with
   the same stem where needed.

## Actions Taken

1. Created this log file.
2. Read `basicpy/index.md`; found 13 exercises with the following IDs and stems:
   - `basicpy-sliding`  → `sliding.py`
   - `basicpy-catadd`   → `catadd.py` + `catadd.txt`
   - `basicpy-andor`    → `andor.py`
   - `basicpy-formula`  → `formula.py`
   - `basicpy-sortnone` → `sortnone.py`
   - `basicpy-indent`   → `indent.py` + `indent.txt`
   - `basicpy-noreturn` → `noreturn.py`
   - `basicpy-nokey`    → `nokey.py` + `nokey.txt`
   - `basicpy-streq`    → `streq.py` + `streq.json`
   - `basicpy-recurse`  → `recurse.py`
   - `basicpy-mutable`  → `mutable.py`
   - `basicpy-trailing` → `trailing.py` + `trailing.txt`
   - `basicpy-strmeth`  → `strmeth.py`
   - `basicpy-mutiter`  → `mutiter.py`
3. Created all files listed above in `basicpy/`.
